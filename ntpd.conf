# /etc/ntp.conf, configuration for ntpd; see ntp.conf(5) for help
listen on 10.0.0.1
driftfile /ntp.drift

#server 127.127.28.0 minpoll 4 maxpoll 4 prefer

server 0.north-america.pool.ntp.org iburst minpoll 10
server 1.north-america.pool.ntp.org iburst minpoll 10
server 2.north-america.pool.ntp.org iburst minpoll 10
server 3.north-america.pool.ntp.org iburst minpoll 10
